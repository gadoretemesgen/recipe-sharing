- name: Deploy frontend to Cloud Run
  uses: google-github-actions/deploy-cloudrun@v2
  with:
    service: recipe-sharing-frontend
    source: ./frontend
    project_id: ${{ secrets.GCP_PROJECT_ID }}
    region: ${{ secrets.GCP_REGION }}
    flags: '--allow-unauthenticated'
